# AGENT.md

本文件是 Aurora Racing 官网的长期协作规范，供车队成员、维护者、外部贡献者和 AI 编程助手共同遵守。任何改动都应优先服务于网站的真实目标：稳定、专业、快速地展示深圳技术大学极光电动方程式车队的品牌、赛车、成员、赞助商与赛事故事。

## 1. 项目定位

- 项目名称：Aurora Racing
- 官网域名：aurora-racing.com
- 所属团队：深圳技术大学极光电动方程式车队
- 当前形态：纯前端宣传展示网站
- 当前部署：
  - 阿里云轻量应用服务器，通常通过宝塔面板上传 `dist/` 更新正式站点
  - GitHub 仓库面向海外协作和代码托管
  - Vercel 部署用于海外访问、预览或备用发布
- 长期方向：
  - 继续保持高质量的宣传展示能力
  - 随车队需求增长，逐步引入后端、数据库、管理后台、内容管理、表单、赞助商资料管理等能力
  - 项目架构应为未来扩展留出空间，但不要为了暂时不存在的需求过度设计

## 2. 当前技术栈

- React 19
- TypeScript
- Vite
- Tailwind CSS v4
- Motion，导入路径使用 `motion/react`
- Lucide React
- Vercel Analytics 与 Speed Insights
- 图片资源以 WebP 为主，集中存放在 `public/images/`

常用命令：

```bash
npm install
npm run dev
npm run lint
npm run build
npm run preview
```

脚本含义：

- `npm run dev`：本地开发，默认监听 `3000` 端口并允许局域网访问。
- `npm run lint`：执行 TypeScript 静态检查，当前实际命令为 `tsc --noEmit`。
- `npm run build`：构建生产产物到 `dist/`。
- `npm run preview`：本地预览生产构建。
- `npm run clean`：清理 `dist/`。

## 3. 目录约定

```text
public/
  images/
    cars/        历代赛车图片
    heritage/    车队历史图片
    hero/        首页首屏图片
    logos/       车队 Logo 与 favicon
    partners/    赞助商 Logo
    store/       商店或购买二维码
    team/        队员照片
src/
  components/    页面区块与复用组件
  App.tsx        单页应用结构
  main.tsx       React 入口
  index.css      Tailwind v4 主题变量与全局样式
dist/            生产构建产物，不手写修改
```

当前页面以单页锚点区块组织：

- `Home`：首页 Hero、赛事倒计时、社交入口
- `Partners`：赞助商展示
- `Specs`：历代赛车与参数
- `Heritage`：车队历史
- `Team`：队员展示
- `Contact`：页脚、社交媒体、备案信息、商店二维码

新增大型模块时，优先放在 `src/components/` 下，并保持一个组件承担一个清晰页面区块。若未来组件数量明显增长，再考虑拆分为 `src/components/<section>/`、`src/data/`、`src/lib/`、`src/hooks/` 等目录。

## 4. 开发原则

1. 真实内容优先  
   网站是车队对外名片。赛车数据、队员姓名、赞助商 Logo、赛事成绩、备案信息和联系方式必须准确，不能用临时文字、占位数据或未经确认的信息冒充正式内容。

2. 稳定发布优先  
   官网上线后，任何改动都要考虑移动端、桌面端、国内服务器和 Vercel 的表现。不要只在本地一个视口看起来正常就发布。

3. 小步提交  
   每次改动尽量围绕一个目的：修复一个问题、更新一批内容、增加一个区块或优化一个体验。避免把无关重构、图片替换和功能开发混在一起。

4. 尊重现有风格  
   当前网站风格是黑白主色、极光紫点缀、电动方程式赛车科技感、强视觉首屏、大图展示、细节动效。新增 UI 应延续这个方向，而不是随意引入另一套视觉语言。

5. 不手写构建产物  
   `dist/` 是 `npm run build` 生成的生产产物。不要直接修改 `dist/` 内文件来修功能。功能和样式必须从 `src/`、`public/`、`index.html` 等源文件修改后重新构建。

6. 保持未来可迁移  
   当前是纯前端，但后续可能接入后端和数据库。新增功能时要避免把数据、展示和业务流程缠死在一个巨大组件里。数据量明显增长时，应逐步抽离到数据文件、API 层或 CMS。

## 5. TypeScript 与 React 规范

- 保持 `strict` 友好，避免 `any`。如果必须使用宽类型，应说明原因并尽量缩小范围。
- 组件使用函数组件。
- React Hooks 只在组件或自定义 Hook 顶层调用。
- 副作用必须清理：
  - `addEventListener` 对应 `removeEventListener`
  - `setInterval` / `setTimeout` 对应清理
  - 图片预加载、媒体查询监听、滚动监听都要在 `useEffect` 中收尾
- 不要在渲染期间直接读写 DOM 或全局状态。
- 事件名、锚点、图片路径等跨组件约定应集中说明或抽离常量，避免隐藏耦合。
- 当前 `Specs` 与 `Navbar` 通过 `changeCarYear` 自定义事件联动。若未来同类交互增多，应考虑更清晰的状态管理方式。
- 列表渲染使用稳定 `key`，不要在会重排的数据上随意用数组下标。
- 继续使用 `motion/react`，不要混用旧的 `framer-motion` 导入路径。

## 6. 样式与 Tailwind 规范

- 全局主题变量优先写在 `src/index.css` 的 `@theme` 中。
- 现有核心品牌色：
  - `aurora-black`: `#000000`
  - `aurora-white`: `#FCFCFC`
  - `aurora-purple`: `#440381`
- 组件内部优先使用 Tailwind 工具类。
- 复杂、复用或需要伪元素的样式可写在 `index.css`，例如 loader、滚动条、特殊玻璃效果、时间线效果。
- 不要随意新增大量一次性颜色。若确实需要新色，应确认它服务于赛车、电动、科技或品牌表达，并尽量沉淀成变量。
- 避免在多个组件中复制同一大段样式。如果某种按钮、图标入口、卡片或标题样式重复出现，应考虑抽成组件或工具类。
- 响应式设计至少覆盖：
  - 手机：`< 768px`
  - 平板或窄桌面：`768px - 1024px`
  - 桌面：`>= 1024px`
  - 大屏：`>= 1440px`

## 7. UI 与体验要求

1. 首屏必须明确  
   打开网站第一屏应立即传达 AURORA Racing、电动方程式赛车、深圳技术大学、速度与科技感。Hero 图片是核心资产，不要弱化为普通背景装饰。

2. 移动端不是附属品  
   车队成员、赞助商、校内外访客很可能从手机访问。所有新增模块必须优先检查移动端：
   - 文本不溢出
   - 按钮可点击区域足够
   - 图片不压扁、不遮挡主体
   - 菜单、轮播、展开面板可触控

3. 动效应服务内容  
   动效可以强化速度、机械、科技和层次感，但不能拖慢首屏、干扰阅读或造成明显卡顿。大面积动画应考虑 `useReducedMotion` 或降低移动端动效强度。

4. 可访问性最低要求  
   - 图片必须有合适的 `alt`，纯装饰图可使用空 `alt`
   - 按钮必须有可理解的文本或 `aria-label`
   - 展开/折叠状态使用 `aria-expanded`
   - 链接到外部站点使用 `target="_blank"` 时必须带 `rel="noopener noreferrer"`
   - 不要只依赖颜色表达状态

5. 字体和排版  
   - 展示标题可使用 `font-display`
   - 正文和说明使用 `font-sans`
   - 赛车编号、年份、技术参数可使用等宽或强机械感排版
   - 中英文混排要检查行高、断行和移动端显示

6. 品牌气质  
   视觉关键词：电动、速度、极光、工程、年轻、国际化、可信赖。避免廉价科幻、过度霓虹、杂乱阴影和无意义装饰。

## 8. 内容与编码规范

- 所有源码文件应使用 UTF-8 编码。
- 中文内容必须在编辑器、浏览器和构建产物中都能正确显示。
- 当前项目中已有部分中文文本看起来存在编码损坏。后续修复内容时，应优先恢复真实中文，而不是继续复制乱码。
- 面向海外的内容应保持英文可读，至少核心导航、首屏、赛车标题、社交链接和 SEO 描述要自然。
- 中文与英文内容都应避免夸张失实宣传。赛事成绩、奖项、赞助关系必须经过确认。
- 备案号、公安备案号、学校地址等合规信息发布前必须核对。
- 日期和赛事倒计时必须使用明确时区。涉及海外比赛时，在代码注释或数据里写明比赛当地时间。

## 9. 静态资源规范

- 图片优先使用 WebP。
- 首页首屏图必须同时考虑桌面和移动端，当前约定：
  - `/images/hero/hero-desktop.webp`
  - `/images/hero/hero-mobile.webp`
- 关键图片应写 `width`、`height`、`decoding`，非首屏图片使用 `loading="lazy"`。
- 首屏 LCP 图片不要懒加载，应保持预加载或高优先级策略。
- Logo、赞助商 Logo、赛车图、队员照片命名应稳定，方便替换。
- 不要把超大原图直接放入项目。上传前应压缩、裁切、转 WebP，并检查清晰度。
- 替换赞助商 Logo 时必须确认：
  - 赞助等级是否正确
  - Logo 是否清晰
  - 透明背景是否正常
  - 深色或浅色背景下是否可辨认
- 替换队员照片时必须确认：
  - 文件名和代码引用一致
  - 人像裁切不遮脸
  - 移动端卡片显示正常
  - `TEAM_IMAGE_FALLBACK` 仍可兜底

## 10. 性能要求

发布前至少关注：

- 首屏图片体积
- 移动端首屏加载速度
- 是否引入了过大的新依赖
- 是否出现明显布局偏移
- 是否有未清理的定时器或监听器
- 是否有控制台报错
- 是否有无意义重复渲染或重型动画

建议目标：

- 保持首页 LCP 图片明确、压缩良好、尺寸匹配。
- 不为了一个简单交互引入大型库。
- 新增图片先压缩，再提交。
- 对隐藏内容、折叠赞助商、队员图片等使用懒加载。
- 保持 Lighthouse 报告作为优化参考，但不要为了分数牺牲真实内容和品牌表达。

## 11. SEO 与分享规范

`index.html` 中的基础 SEO 信息必须保持准确：

- `<title>` 应包含 AURORA Racing
- `description` 应说明这是深圳技术大学 Formula Student 电动方程式车队官网
- `og:title`、`og:description`、`og:image` 应适合微信、海外社媒和搜索引擎分享
- `theme-color` 与品牌主色一致
- favicon 使用 `public/images/logos/` 下的正式资源

未来如果引入多页面、博客或新闻，应为每个页面提供独立标题、描述、封面图和结构化数据。

## 12. 安全与隐私

- 不要提交真实 `.env`、密钥、服务器密码、宝塔面板账号、数据库密码或云厂商凭据。
- `.env.example` 只放示例变量，不放真实值。
- 当前项目里存在 `GEMINI_API_KEY` 示例配置，但纯前端官网不应在浏览器端暴露任何真正的私钥。
- 如果未来需要调用 AI、邮件、数据库或支付接口，必须通过后端代理或服务端函数处理，不要把密钥放进前端包。
- 表单、报名、赞助咨询等用户输入功能上线前，必须考虑：
  - 验证与限流
  - 垃圾提交防护
  - 日志脱敏
  - 数据备份
  - 隐私声明

## 13. 本地开发流程

推荐流程：

```bash
npm install
npm run dev
```

开发时：

1. 从源文件修改，不直接改 `dist/`。
2. 检查桌面和移动端。
3. 有图片替换时，确认图片路径、大小、清晰度和首屏表现。
4. 有中文内容时，确认没有乱码。
5. 提交前运行：

```bash
npm run lint
npm run build
```

如果构建失败，不要发布。先修复 TypeScript、路径、资源或依赖问题。

## 14. 上线流程

### 14.1 阿里云轻量服务器 / 宝塔面板

当前正式站常用流程：

1. 本地确认代码和内容。
2. 运行检查：

```bash
npm run lint
npm run build
```

3. 确认 `dist/` 已生成。
4. 本地可选预览：

```bash
npm run preview
```

5. 将 `dist/` 中的内容上传到宝塔面板对应的网站根目录。
6. 不要上传 `src/`、`node_modules/`、`.env`、`.git/` 到生产网站目录。
7. 上传后访问 `https://aurora-racing.com` 检查：
   - 首页是否加载
   - 图片是否正常
   - 导航锚点是否正常
   - 移动端菜单是否正常
   - 赞助商与队员图片是否正常
   - 备案链接是否正常
   - 控制台是否有明显错误

建议每次正式上传前备份旧版本静态文件，便于紧急回滚。

### 14.2 Vercel

- Vercel 通常应从 GitHub 仓库自动部署。
- 推送到主分支前必须确保 `npm run lint` 和 `npm run build` 通过。
- 预览部署可用于检查海外访问、分享图和移动端表现。
- Vercel Analytics 和 Speed Insights 可作为性能观察来源，但不能替代真实设备检查。

### 14.3 GitHub

- 主分支保持可构建。
- 不提交 `node_modules/`、真实 `.env`、临时日志、构建压缩包。
- `dist/` 当前被 `.gitignore` 忽略，除非团队明确决定使用 GitHub Pages 或其他需要提交构建产物的流程，否则不要提交 `dist/`。
- 提交信息应简洁明确，例如：
  - `fix: correct sponsor logo layout`
  - `feat: add 2026 car section`
  - `docs: add project agent guide`
  - `perf: optimize hero images`

## 15. 未来后端与数据库演进原则

当纯前端不足以支撑需求时，再逐步引入后端。优先从真实需求出发，例如：

- 在线报名或招新表单
- 赞助咨询表单
- 新闻动态或赛事日志
- 队员和赞助商内容管理
- 多语言内容管理
- 后台管理权限
- 图片上传和媒体库
- 数据统计与访问分析

演进原则：

1. 先定义数据模型，再选技术  
   不要先为了学习某个框架而重构官网。先明确要管理什么数据、谁来维护、多久更新一次、是否需要权限。

2. 前后端边界清晰  
   前端负责展示和交互，后端负责数据、权限、密钥、表单处理、文件上传和日志。

3. 密钥永远留在服务端  
   数据库连接串、邮件服务密钥、AI 密钥、云存储密钥都不能进入前端包。

4. 管理后台要有权限  
   如果未来允许非开发成员更新内容，必须设计登录、角色、审计和回滚。

5. 内容可以先数据化  
   在正式引入数据库前，可先把赛车、队员、赞助商、历史内容抽到 `src/data/`，让组件只负责展示。这是平滑过渡到 CMS 或 API 的第一步。

6. 生产数据库必须备份  
   只要网站开始保存用户或车队运营数据，就必须有备份、恢复和权限管理。

## 16. AI 协作规则

AI 助手在修改本项目时必须：

- 先阅读相关文件，再做修改。
- 只改与任务相关的文件。
- 不覆盖用户已有改动。
- 不直接修改 `dist/` 来修复源代码问题。
- 不凭空编造车队成绩、赞助商、成员姓名、赛事安排或联系方式。
- 修改 UI 后尽量运行 `npm run lint` 和 `npm run build`。
- 对涉及视觉的改动，应检查桌面和移动端效果。
- 发现中文乱码、资源缺失、构建失败或部署风险时，必须明确指出。
- 如果需要新增依赖，应说明原因，并优先选择轻量、长期维护、与当前技术栈兼容的包。
- 如果任务会影响上线流程、SEO、备案信息、外链、表单或数据安全，应更加谨慎。

## 17. 代码审查清单

提交或上线前检查：

- `npm run lint` 是否通过
- `npm run build` 是否通过
- 首页首屏是否正常
- 移动端导航是否正常
- 所有图片是否加载
- 赞助商 Logo 是否完整
- 队员照片和姓名是否正确
- 赛车年份、编号、参数、成绩是否正确
- 中文是否无乱码
- 外链是否可打开
- `target="_blank"` 是否有 `rel="noopener noreferrer"`
- 控制台是否无明显错误
- `dist/` 是否由最新源代码构建
- 是否没有提交密钥、日志、临时文件或无关大文件

## 18. 长期维护建议

- 尽快修复源码中已出现的中文乱码，并确认编辑器统一使用 UTF-8。
- 将赛车数据、队员数据、赞助商数据、历史内容逐步抽离到 `src/data/`，降低组件体积。
- 为图片压缩建立固定流程，例如统一使用 Sharp 或其他工具生成 WebP。
- 为正式发布建立版本记录，记录每次上线时间、改动内容和回滚方式。
- 随着内容增多，考虑增加多语言策略，至少保证中文和英文核心信息同步。
- 当车队有稳定内容更新需求时，再考虑 CMS、后端和管理后台。
- 当访问量或功能复杂度增长时，重新评估服务器、CDN、对象存储、缓存和监控方案。

这份文件不是一次性说明书，而是项目长期协作的地基。若项目技术栈、部署方式、团队流程或品牌规范发生变化，应同步更新本文件。
